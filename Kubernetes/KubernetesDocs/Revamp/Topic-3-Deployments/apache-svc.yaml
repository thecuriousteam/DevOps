apiVersion: v1
kind: Service
metadata:
  labels:
    app: webserver-svc
  name: webserver-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: apache-webserver
  type: NodePort